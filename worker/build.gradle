// disable jar task. it doesn't contain manifest and fails heroku
jar {
    enabled = false
}

dependencies {
    implementation('org.springframework.boot:spring-boot-starter-aop') // required for retry
    implementation('org.springframework.retry:spring-retry')
}
